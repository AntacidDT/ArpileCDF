dependencies:
  espressif/esp_wifi_remote:
    version: ^1.0.0
    rules:
    - if: target in [esp32p4]
  espressif/esp_hosted:
    version: ^2.4.0
    rules:
    - if: target in [esp32p4]
  idf:
    version: '>=5.3.0'
  espressif/cjson: '*'
